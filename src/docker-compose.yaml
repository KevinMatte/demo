# See https://docs.docker.com/reference/compose-file/

services:
  demo_ui:
# #if mounted
    image: ${DEMO_IMAGE}:latest
# #else
#    image: localhost:5000/${DEMO_IMAGE}:${DEMO_VERSION}
# #endif
    container_name: demo_ui
# #if mounted or local
    build: images/demo_ui
# #endif
    ports:
      - name: web
        target: 80
        published: "8080"
        protocol: tcp
        app_protocol: http
        mode: host

# #if mounted
#    volumes:
#      - ${WWW_PATH}:/var/www
# #endif

    networks:
      - frontend
      - backend

#  db:
#    image: mariadb
#    networks:
#      - backend

networks:
  frontend:
  backend:
